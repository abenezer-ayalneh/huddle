# Env & secrets
.env
.env.*
!.env.example
!.env.prod.example
!.env.tunnel.example

# SSH deploy keys — never commit private keys (see docs/RUNBOOK_CICD.md)
huddle_deploy_key
huddle_deploy_key.pub

# Node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Builds / output
dist/
build/
.next/
out/
coverage/

# Caches
.turbo/
.cache/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code tooling runtime (scheduled-task lock) — .claude/launch.json stays tracked
.claude/scheduled_tasks.lock

# LiveKit / infra runtime data
infra/data/
*.log

# Graphify knowledge graph (regenerable)
graphify-out/
infra/dev-tls/
